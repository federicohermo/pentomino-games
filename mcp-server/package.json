{
  "name": "mcp-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "MCP server que ejecuta el dominio de pentomino-games. Tooling local: no entra al bundle ni al deploy.",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "start": "node src/index.ts",
    "typecheck": "tsc",
    "test": "pnpm run typecheck && node --test --experimental-test-coverage --test-coverage-include=\"src/**\" --test-coverage-exclude=\"src/__tests__/**\" --test-coverage-exclude=\"src/index.ts\" --test-coverage-lines=100 --test-coverage-branches=100 --test-coverage-functions=100 \"src/__tests__/*.test.ts\""
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.2.0",
    "typescript": "~5.8.3"
  },
  "devDependencies": {
    "@types/node": "^24.3.1"
  }
}
