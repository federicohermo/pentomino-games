/**
 * Dimensiones del tablero.
 *
 * `GRID_W` no es solo ancho: **el eje X del tablero es tiempo**, asi que 10 es
 * tambien la cantidad de posiciones dentro del compas (spec 004). Cambiarlo
 * cambia la resolucion ritmica del instrumento, no solo cuanto entra.
 */
export const GRID_W = 10;
export const GRID_H = 6;
